/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DirectoryCreatedResponse } from '../../models/directory-created-response';
import { NewRootDirectoryRequest } from '../../models/new-root-directory-request';

export interface LearningDirectoriesNewPost$Json$Params {
      body?: NewRootDirectoryRequest
}

export function learningDirectoriesNewPost$Json(http: HttpClient, rootUrl: string, params?: LearningDirectoriesNewPost$Json$Params, context?: HttpContext): Observable<StrictHttpResponse<DirectoryCreatedResponse>> {
  const rb = new RequestBuilder(rootUrl, learningDirectoriesNewPost$Json.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'text/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DirectoryCreatedResponse>;
    })
  );
}

learningDirectoriesNewPost$Json.PATH = '/learning/directories/new';
