/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DirectoryTreeDto } from '../../models/directory-tree-dto';

export interface ClassworkDirectoriesStudyCategoryGet$Json$Params {
  studyCategory: string;
}

export function classworkDirectoriesStudyCategoryGet$Json(http: HttpClient, rootUrl: string, params: ClassworkDirectoriesStudyCategoryGet$Json$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<DirectoryTreeDto>>> {
  const rb = new RequestBuilder(rootUrl, classworkDirectoriesStudyCategoryGet$Json.PATH, 'get');
  if (params) {
    rb.path('studyCategory', params.studyCategory, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'text/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<DirectoryTreeDto>>;
    })
  );
}

classworkDirectoriesStudyCategoryGet$Json.PATH = '/classwork/directories/{studyCategory}';
