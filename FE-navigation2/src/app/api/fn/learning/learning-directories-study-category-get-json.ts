/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DirectoryTreeDto } from '../../models/directory-tree-dto';

export interface LearningDirectoriesStudyCategoryGet$Json$Params {
  studyCategory: string;
}

export function learningDirectoriesStudyCategoryGet$Json(http: HttpClient, rootUrl: string, params: LearningDirectoriesStudyCategoryGet$Json$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<DirectoryTreeDto>>> {
  const rb = new RequestBuilder(rootUrl, learningDirectoriesStudyCategoryGet$Json.PATH, 'get');
  if (params) {
    rb.path('studyCategory', params.studyCategory, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'text/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<DirectoryTreeDto>>;
    })
  );
}

learningDirectoriesStudyCategoryGet$Json.PATH = '/learning/directories/{studyCategory}';
