/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ItemWithNameDto } from '../../models/item-with-name-dto';

export interface LearningDirectoryIdParentsGet$Plain$Params {
  id: number;
}

export function learningDirectoryIdParentsGet$Plain(http: HttpClient, rootUrl: string, params: LearningDirectoryIdParentsGet$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<ItemWithNameDto>>> {
  const rb = new RequestBuilder(rootUrl, learningDirectoryIdParentsGet$Plain.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'text', accept: 'text/plain', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<ItemWithNameDto>>;
    })
  );
}

learningDirectoryIdParentsGet$Plain.PATH = '/learning/directory/{id}/parents';
